apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: bpe-qrcode
spec:
  hosts:
    - bpe-qrcode
  http:
    - route:
        - destination:
            host: bpe-qrcode
            subset: v1
          weight: 90
        - destination:
            host: bpe-qrcode
            subset: v2
          weight: 10
---